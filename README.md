# presence-cards

Discord presence cards rendered as SVG.

## Requirements

- Python 3.11+
- [uv](https://docs.astral.sh/uv/) for dependency management
- A Discord bot token

## Setup

Install dependencies from the lockfile:

```bash
uv sync
```